# Banco de dados local
DATABASE_URL=postgres://postgres:85885885@localhost:5432/wildhub?sslmode=disable

# Redis local (caso você não tenha Redis, pode usar esse fake temporário)
REDIS_URL=redis://localhost:6379

# JWT (gera uma string aleatória se quiser)
JWT_SECRET=minha_chave_super_secreta_123

# API da OpenAI (se você não tiver, pode deixar em branco ou usar uma fake)
OPENAI_API_KEY=sk-fake1234567890

# API Evolution (ajuste se souber o endpoint e a chave)
EVOLUTION_API_URL=https://api.evolution.fake/v1
EVOLUTION_API_KEY=fake-api-key-123
